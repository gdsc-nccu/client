// hooks
import { useRouter } from "next/navigation";
// components
// styles
import classnames from "classnames";

const Home = () => {
  // const isLogin = useAppSelector((state) => state.User.isLogin);
  // const { login } = useUser();

  return <div className={classnames("h-full w-full")}>wlwlwllw</div>;
};

export default Home;
