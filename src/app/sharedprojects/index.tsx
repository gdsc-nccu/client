// hooks
import { useUnitedResize } from "../../hooks";
// components
// styles
import classnames from "classnames";

const Home = () => {
  return <div className={classnames("h-full w-full")}></div>;
};

export default Home;
